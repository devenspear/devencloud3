/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  distDir: 'dist',
  images: {
    unoptimized: true
  },
  basePath: '',
  assetPrefix: ''
}

export default nextConfig
